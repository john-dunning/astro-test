---
import Page from "./Page.astro";
import Header from "./Header.astro";

interface Props {
	title?: string;
}

const { title } = Astro.props;
---

<Page title={title}>
	<Header title={title} />
	<main>
		<slot />
	</main>
</Page>

<style>
	main {
		margin-bottom: 5rem;
	}
</style>
